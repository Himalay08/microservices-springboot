server:
  port: 8085
  
spring:
  application:
    name: CONFIG-SERVER  
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Himalay08/microservices-configserver
          clone-on-start: true      
        bootstrap: true